
export const TOUCH_CONFIG = {
  // Максимальное время между нажатиями для регистрации двойного тапа (мс)
  DOUBLE_TAP_DELAY: 300,
  
  // Время удержания для регистрации долгого нажатия (мс)
  LONG_PRESS_DELAY: 500,
  
  // Минимальное расстояние свайпа для регистрации (пиксели)
  SWIPE_THRESHOLD: 50,
  
  // Максимальное время выполнения свайпа (мс)
  SWIPE_TIMEOUT: 500,

  // Минимальный размер интерактивных элементов (согласно гайдлайнам доступности)
  MIN_TARGET_SIZE: 44,
  
  // Вибрация (Haptic Feedback) при успешных действиях (если поддерживается)
  HAPTIC_ENABLED: true,
  HAPTIC_PATTERNS: {
    SUCCESS: [10],
    ERROR: [50, 50, 50],
    LONG_PRESS: [20]
  }
};
